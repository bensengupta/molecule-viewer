name: Keep alive cron job
on:
  schedule:
    - cron: '0 12 * * *' # daily
jobs:
  cron:
    runs-on: ubuntu-latest
    steps:
      - name: Call endpoint to keep the database alive
        run: |
          curl --request PUT \
            --url '${{ vars.SITE_URL }}/api/keep-alive'
